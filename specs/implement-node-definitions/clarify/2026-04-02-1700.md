# Clarify Session 2026-04-02 17:00

## Idea

WaitNode execute() 修正：將 duration + unit 轉換為 Unix timestamp 並排程。

目前 WaitNode::execute() 有骨架但核心邏輯缺失（嘗試透過 ParamHelper.try_get_param('timestamp') 取一個不存在的參數），實際前端傳入的是 duration（數值字串）+ unit（minutes/hours/days）。

需要：
1. 實作 duration + unit -> Unix timestamp 轉換邏輯
2. 新增參數驗證與錯誤處理（對齊 WaitUntilNode 的防禦性程式碼風格）
3. 確保 scheduled=true 正確回傳

## 需求分析結論

經過完整原始碼與規格審閱，此需求**無需澄清**。原因：

1. **影響範圍明確**：僅修改一個檔案 `WaitNode.php`
2. **參數來源明確**：直接從 `$node->params` 取值（不用 ParamHelper），前端定義已在 WaitNode 的 form_fields 中
3. **錯誤訊息明確**：Issue 中已列出所有 7 個情境的完整錯誤訊息
4. **參考實作存在**：WaitUntilNode 已提供完整的防禦性程式碼模式
5. **無新 Library**：全部使用既有的 Action Scheduler API
6. **無歧義用詞**：duration/unit/timestamp 在本專案中語義唯一

## 產出

- `features/nodes/wait-node-execute.feature` -- 完整的 WaitNode execute 行為規格（8 個 Example，涵蓋 7 個 Rule）
- 更新 `activities/implement-node-definitions.activity` -- 新增 STEP:5 引用 wait-node-execute.feature，後續步驟重新編號

## Q&A

（本次無澄清問題，需求已完整明確）
