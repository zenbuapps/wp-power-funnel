# Clarify Session 2026-03-30 15:00

## Idea

功能「觸發條件下拉選單動態化」已實作完成，需要將規格文件與實際實作同步。

## 變更紀錄

### 比對結果

逐一比對以下實作檔案與規格文件：

| 實作檔案 | 對應規格 | 比對結果 |
|---------|---------|---------|
| `TriggerPointApi.php` (GET /trigger-points) | `api/api.yml` | 一致 |
| `ETriggerPoint.php` (REGISTRATION_CREATED) | `entity/erm.dbml` | 一致 |
| `Repository::get_trigger_points()` | `.feature` Background | 一致 |
| `QueryTriggerPointsTest.php` (5 tests) | `.feature` Rules/Examples | 一致 |
| `useTriggerPoints.ts` (hook/name 結構) | `api/api.yml` schemas | 一致 |
| `types/index.ts` (移除硬編碼常數) | `.feature` 動態化設計 | 一致 |
| `flowSerializer.ts` (labelMap 參數) | `.activity` STEP:3 | 一致 |
| `useColumns.tsx` (動態 label) | `.activity` STEP:1 | 一致 |
| `Bootstrap.php` (register TriggerPointApi) | `api/api.yml` endpoint | 一致 |

### 執行的變更

- `features/workflow-rule/query-trigger-points.feature`: 移除 `@ignore` tag（功能已實作完成並通過測試）

### 未變更的項目（已一致）

- `api/api.yml`: endpoint 路徑、HTTP method、回應格式、schema 定義均與實作一致
- `entity/erm.dbml`: 正確描述為運行時資料結構（非持久化），Enum 值與 ETriggerPoint 一致
- `activities/trigger-points-dynamic-select.activity`: 流程描述與前後端實作一致
- `specs/actors/admin.md`: manage_options 權限描述正確
- `specs/actors/system.md`: ETriggerPoint + apply_filters 描述正確
